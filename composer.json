{
    "name": "abacusresearch/abacus-cloud-template",
    "type": "project",
    "require": {
        "php": "*"
    },
    "scripts": {
        "post-install-cmd": [
            "npm install -g bower",
            "bower install",
            "npm install"
        ]
    }
}
